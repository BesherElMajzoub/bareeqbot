# بريق (Bariq) — ملف التسليم الكامل

> **الغرض من هذا الملف:** إعطاء صورة كاملة عن المشروع لشخص لم يعمل عليه من قبل، وتحديد **ما هو مبنيّ فعلًا**،
> و**ما هو ناقص**، و**الإجراءات المطلوبة على منصة Meta Developer** لتشغيل التطبيق فعليًا.
>
> **آخر تحديث:** 2026-07-11
> **النطاق الرسمي:** `https://bareeqplatform.site`

---

## 1) ما هو بريق؟ (الهدف)

**بريق (Bariq)** منصّة SaaS متعددة المستأجرين (Multi-tenant) تقوم بـ**أتمتة الردود على فيسبوك وإنستغرام**
عبر واجهة **Meta Graph API** الرسمية.

**ماذا تفعل عمليًا؟**
صاحب المتجر/الصفحة يربط صفحته، ويُنشئ **قواعد أتمتة** (مثلًا: "أي تعليق يحتوي كلمة *السعر* → ردّ عام: تفضل السعر كذا،
+ رسالة خاصة بالتفاصيل"). عندما يعلّق أي شخص، يستقبل بريق الحدث من Meta فورًا، ويطابقه مع القواعد، ويرسل الرد تلقائيًا.

**الأسطح المدعومة (Surfaces):**
| السطح | الوصف | الرد |
|---|---|---|
| `post_comment` | تعليق على منشور (فيسبوك + إنستغرام) | رد عام + رسالة خاصة |
| `story_reply` | رد على ستوري | رسالة خاصة (DM) |
| `story_mention` | إشارة (منشن) في ستوري | رسالة خاصة (DM) |

**السوق المستهدف:** سوريا والسعودية — **الواجهة عربية أولًا (RTL)**، والعملة **الليرة السورية (SYP)**.

**نموذج العمل:** اشتراكات بباقات. **الدفع يدوي** (تحويل خارجي → المستخدم يرسل طلب اشتراك → الأدمن يوافق فيُفعَّل الاشتراك).
مقياس الحصة (Quota) هو **عدد الصفحات/الحسابات المربوطة** المسموح بها في الباقة.

---

## 2) الحزمة التقنية (Tech Stack)

- **الخلفية:** PHP 8.3 · **Laravel 13** · MySQL 8
- **الواجهة:** Inertia v3 + **React 19** + TypeScript + Tailwind v4 + Wayfinder (مسارات مولّدة تلقائيًا)
- **المصادقة:** **Laravel Fortify** (تحقق بريد، 2FA، Passkeys) — لا يوجد Sanctum (اللوحة SPA مُخدَّمة من السيرفر)
- **الاختبارات:** **Pest** — ✅ **150 اختبار تمر بنجاح**
- **الطوابير (Queues):** `database` حاليًا. **كل نداء خارجي لـ Meta وكل webhook يُنفَّذ داخل Job** (مع إعادة محاولة).
- **الحزم:** `spatie/laravel-permission` (أدوار مع Teams), `spatie/laravel-data` (DTOs), `spatie/laravel-query-builder`, `predis/predis`

### أوامر مهمة
```bash
composer dev          # يشغّل: serve + queue:listen + vite معًا  ← لازم للتطوير
composer test         # pint + phpstan + كل الاختبارات
php artisan migrate   # ترحيل قاعدة البيانات
php artisan db:seed   # الأدوار + الباقات + حساب الأدمن
```

> ⚠️ **بدون `queue:listen` شغّالًا، لن يُرسَل أي رد أبدًا** — الأحداث تُحفظ فقط ولا تُعالَج. هذا أكثر خطأ شائع.

---

## 3) المعمارية — القواعد غير القابلة للتفاوض

1. **Controllers نحيفة، Services سمينة.** لا منطق عمل في الـ Controller إطلاقًا.
2. **Action classes** لكل عملية كتابة (`app/Actions/…`)، **Services** للتكامل الخارجي (`MetaGraphClient`, `RuleMatcher`, `ReplyDispatcher`).
3. **DTOs** (`spatie/laravel-data`) لكل بيانات تعبر الحدود (payloads الويبهوك، ردود Meta).
4. **Policy + Form Request** لكل قرار صلاحية وكل مُدخل.
5. **كل التوكنات مشفّرة في قاعدة البيانات** (`encrypted` cast) — **ولا تُسجَّل في اللوج إطلاقًا**.
6. **Idempotency إجباري:** يستحيل الرد مرتين على نفس التعليق (مضمون بـ unique index على `reply_logs`).

### تعدد المستأجرين (Multi-tenancy) — الأهم
عزل على مستوى الصف (Row-level). كل جدول مملوك لمستأجر فيه `tenant_id`، ويُطبَّق `TenantScope` تلقائيًا عبر
الـ trait `BelongsToTenant`.

> 🔴 **تحذير حرج:** في **jobs الويبهوك لا يوجد مستخدم مُصادَق (`auth()`)**. يجب استخراج المستأجر من
> معرّف صفحة Meta ← `ChannelConnection` ← `tenant_id`، ثم ضبطه يدويًا **قبل** لمس أي نموذج tenant-scoped.

### الأدوار
- **أدوار المستأجر:** `owner` (كل شيء) · `member` (بلا فوترة)
- **أدوار المنصّة:** `super_admin` · `support` (تُسنَد تحت team سنتينل `0`، ويحملون `users.is_platform_staff = true`)

> ملاحظة مهمة للمطوّر الجديد: **حساب الأدمن (`admin@bariq.test`) لا يملك مستأجرًا**، لذلك لا يستطيع فتح صفحات
> المستأجر (القواعد/القنوات/الفوترة) — هذا **سلوك مقصود وليس خللًا**. للتجربة كصاحب متجر، **سجّل حسابًا جديدًا عاديًا** من `/register`.

---

## 4) ما هو مبنيّ فعلًا ✅ (المراحل الثمانية كلها مكتملة)

| # | المرحلة | الحالة |
|---|---|---|
| 1 | **الأساس** — مصادقة، تعدد مستأجرين، أدوار وصلاحيات، باقات + بذور | ✅ |
| 2 | **الفوترة اليدوية** — طلبات اشتراك، موافقة/رفض من الأدمن، دورة حياة الاشتراك، أمر انتهاء مجدول، خدمة الحصة (Quota) | ✅ |
| 3 | **ربط Meta OAuth** — Facebook Login، اختيار الصفحة/إنستغرام، توكنات مشفّرة، اشتراك الويبهوك عند الربط | ✅ |
| 4 | **استقبال Webhooks** — تحقق GET + POST موقّع (HMAC SHA-256) → تخزين خام → Job → تحليل → حل المستأجر → منع التكرار | ✅ |
| 5 | **محرّك القواعد + ردود فيسبوك** — CRUD للقواعد، مطابقة (أولوية + نطاق + any/exact/contains/regex)، إرسال idempotent، قوالب `{{placeholder}}` | ✅ |
| 6 | **إنستغرام + الستوري** — ردود تعليقات IG، ردود/إشارات الستوري → رسائل خاصة (Send API) | ✅ |
| 7 | **التحليلات** — عدادات حيّة، تجميع ليلي، لوحة المستأجر + لوحة الأدمن، سجل ردود مُفلتر | ✅ |
| 8 | **التصليب** — إدارة المستأجرين (تعليق/تفعيل كمفتاح إيقاف حقيقي)، فاحص أحداث الويبهوك، تحديد معدّل الطلبات، معالجة انتهاء التوكنات | ✅ |

**إضافات حديثة (بعد المراحل الثمانية):**
- ✅ **اشتراك واحد فعّال فقط** لكل مستأجر (لا يمكن إرسال طلب جديد أثناء وجود اشتراك فعّال أو طلب قيد المراجعة).
- ✅ **زر إلغاء الاشتراك** للمستخدم — بعد الإلغاء يستطيع طلب باقة أخرى فورًا.
- ✅ الباقات صارت **بُعدين**: المدّة (1/3/6/12 شهر) × المنصّة (**فيسبوك فقط** أو **فيسبوك + إنستغرام**).
- ✅ العملة **الليرة السورية (SYP)**.
- ✅ صفحات قانونية عامة: `/privacy` · `/terms` · `/data-deletion` (مطلوبة لنشر تطبيق Meta).

---

## 5) 🔴 المشكلة الحالية — لماذا لا تصل التعليقات؟

**تم تشخيصها بشكل قاطع.** كل شيء في الكود سليم، والرابط تم التحقق منه بنجاح من Meta، لكن:

> **تطبيق Meta ما زال في وضع Development (غير منشور).**
>
> سياسة Meta الحالية (مكتوبة حرفيًا في لوحة Webhooks):
> «ستتمكن التطبيقات من تلقي Webhooks **الاختبارية** من لوحة المعلومات **فقط** عند إلغاء نشر التطبيق.
> **لن يتم إرسال أي بيانات تشغيل — بما في ذلك بيانات مسؤولي التطبيق أو المطوّرين أو المختبرين — إلا بعد نشر التطبيق.»**

**الترجمة العملية:**
- ✅ زر **"اختبار"** في لوحة Meta → يصل الحدث (يثبت أن الكود يعمل).
- ❌ **تعليق حقيقي من أي شخص → لا يصل شيء إطلاقًا** حتى يُنشر التطبيق (Live).

**الدليل:** فحص نفق الشبكة أظهر أنّ رابط التحقق نجح (`GET /webhooks/meta → 200` مرتين)، لكن **صفر** طلبات
`POST /webhooks/meta` من فيسبوك رغم التعليق الحقيقي. جدول `webhook_events` = **0**.

**الخلاصة: المطلوب هو إكمال إجراءات Meta ونشر التطبيق — لا يوجد شيء لإصلاحه في الكود.**

---

## 6) ✅ المطلوب منك على منصة Meta Developer (الإجراءات)

**معرّف التطبيق:** `1478364334328367` · **الاسم:** bareeqbot

### الخطوة أ — الإعدادات الأساسية (Basic Settings)
املأ هذه الحقول (الصفحات جاهزة ومرفوعة على النطاق):

| الحقل | القيمة |
|---|---|
| عنوان URL لسياسة الخصوصية | `https://bareeqplatform.site/privacy` |
| عنوان URL لشروط الخدمة | `https://bareeqplatform.site/terms` |
| عنوان URL لتعليمات حذف البيانات | `https://bareeqplatform.site/data-deletion` |
| الفئة (Category) | **Business and pages** |
| أيقونة التطبيق | شعار مربّع PNG (≤ 5MB) |
| نطاقات التطبيق (App Domains) | `bareeqplatform.site` |
| البريد الإلكتروني | `bishermajdoub@gmail.com` |

> ⚠️ **احذف أي قيم قديمة تشير إلى `facebook.com` أو روابط ngrok** — لن تُقبل.

### الخطوة ب — إعداد Webhooks
1. **Products → Webhooks → اختر كائن `Page`**
2. **Callback URL:** `https://bareeqplatform.site/webhooks/meta`
3. **Verify Token:** القيمة الموجودة في `.env` تحت `META_WEBHOOK_VERIFY_TOKEN` (يجب أن تتطابق تمامًا)
4. اضغط **Verify and Save** → يجب أن تظهر علامة نجاح ✅
5. **اشترك بالحقول التالية تحت `Page`** (هذه هي التي يستخدمها الكود فعليًا):

   | الحقل | لماذا |
   |---|---|
   | **`feed`** | 🔴 **الأهم** — تعليقات المنشورات |
   | **`messages`** | الرسائل الخاصة + ردود الستوري |
   | **`messaging_postbacks`** | تفاعلات الأزرار |

6. **لإنستغرام** — أضف منتج **Instagram** واشترك بحقول: **`comments`** · **`messages`** · **`story_insights`**

> 💡 الكود يشترك بهذه الحقول تلقائيًا على مستوى **الصفحة** عند الربط، لكن الاشتراك على مستوى **التطبيق**
> (الخطوة ب) **يدويّ وإلزامي** — الطبقتان مطلوبتان معًا.

### الخطوة ج — الصلاحيات (Permissions) المطلوبة
التطبيق يطلب هذه الصلاحيات عند تسجيل الدخول:

| الصلاحية | الغرض |
|---|---|
| `pages_show_list` | عرض قائمة صفحات المستخدم |
| `pages_read_engagement` | قراءة التعليقات |
| `pages_manage_engagement` | **الرد على التعليقات** |
| `pages_messaging` | **إرسال الرسائل الخاصة** |
| `pages_manage_metadata` | **اشتراك الويبهوك على الصفحة** |
| `business_management` | إدارة أصول الأعمال |

للإطلاق العام تحتاج **App Review** لهذه الصلاحيات (Advanced Access) — Meta ستطلب **فيديو شاشة** يوضح
كيف يستخدم التطبيق كل صلاحية.

### الخطوة د — 🚀 النشر (الأهم)
بدّل التطبيق من **Development → Live** من المفتاح أعلى اللوحة.
**هذه الخطوة وحدها هي ما سيجعل التعليقات الحقيقية تصل.**

---

## 7) قائمة تحقق للاختبار (End-to-End)

بعد إكمال إجراءات Meta:

1. ✅ تأكد أن `composer dev` شغّال (فيه `queue:listen`) — **بدونه لا تُرسل أي ردود**.
2. ✅ سجّل حسابًا جديدًا عاديًا من `/register` (**لا تستخدم حساب الأدمن** — لا يملك مستأجرًا).
3. ✅ فعّل اشتراكًا: اطلب باقة من `/billing` ← وافق عليها من `/admin/subscription-requests`.
4. ✅ اربط الصفحة من `/connections`.
5. ✅ أنشئ قاعدة من `/rules` (اختر الصفحة، نوع المطابقة، نص الرد).
6. ✅ علّق تعليقًا حقيقيًا على منشور بالصفحة **من حساب آخر**.
7. ✅ راقب `/admin/webhook-events` → يجب أن يظهر الحدث، ثم يصل الرد خلال ثوانٍ.
8. ✅ تحقّق من الرد في `/analytics/logs`.

**إن لم يظهر شيء في `/admin/webhook-events`:** المشكلة في إعدادات Meta (نشر التطبيق أو اشتراك حقل `feed`)، وليست في الكود.

---

## 8) ما هو الناقص فعلًا (مؤجَّل بوعي، ليس خللًا)

| البند | الحالة | ملاحظة |
|---|---|---|
| **نشر تطبيق Meta + App Review** | 🔴 **مطلوب الآن** | **هذا هو المانع الوحيد للتشغيل الحقيقي** |
| **بوابة دفع آلية** | ⏸️ مؤجّل | البنية جاهزة خلف واجهة `BillingProvider` — يُضاف مزوّد جديد دون لمس دورة حياة الاشتراك |
| **Horizon** (لوحة الطوابير) | ⏸️ مؤجّل | **يستحيل تشغيله على ويندوز** (يتطلب `ext-pcntl`/`ext-posix`). يُثبَّت في بيئة Linux/Docker: `composer require laravel/horizon` |
| تشغيل الطابور كخدمة دائمة | 🔴 مطلوب للإنتاج | استبدل `queue:listen` بـ **Supervisor/systemd** — وإلا تتوقف الردود عند إغلاق الطرفية |
| **WhatsApp** | ❌ غير مبنيّ | خارج نطاق النسخة الحالية |
| **تدفقات محادثة متعددة الخطوات** | ❌ غير مبنيّ | القواعد حاليًا "محفّز → رد" مباشر |
| **أسعار الباقات الحقيقية** | ⚠️ قيم مؤقتة | الأرقام في `database/seeders/PlanSeeder.php` **placeholder** — استبدلها بالتسعيرة الفعلية ثم: `php artisan db:seed --class=PlanSeeder --force` |
| ميزات (فلترة كلمات نابية، AI، متجر، جدولة نشر) | ❌ غير مبنيّة | خارج النطاق حاليًا بقرار صريح |

---

## 9) خريطة الملفات المهمة

| المسار | الغرض |
|---|---|
| `app/Services/Meta/MetaGraphClient.php` | كل نداءات Meta Graph API + حقول الويبهوك الافتراضية |
| `app/Services/Meta/FacebookOAuthService.php` | تدفّق OAuth + **قائمة الصلاحيات (SCOPES)** |
| `app/Http/Controllers/WebhookController.php` | نقطة استقبال الويبهوك (تحقق GET + استقبال POST) |
| `app/Jobs/ProcessMetaWebhook.php` | معالجة الحدث: حل المستأجر → مطابقة القواعد → إرسال |
| `app/Services/Automation/RuleMatcher.php` | منطق مطابقة القواعد |
| `app/Actions/Connections/ConnectChannel.php` | ربط الصفحة + اشتراك الويبهوك |
| `database/seeders/PlanSeeder.php` | **الباقات والأسعار (عدّلها هنا)** |
| `BARIQ_BUILD_PROMPT.md` | **المصدر الأوحد للحقيقة** لسلوك المنتج |
| `CLAUDE.md` | دليل المعمارية للمطوّرين/الوكلاء |

---

## 10) خلاصة تنفيذية

> **التطبيق مبنيّ بالكامل ومختبَر (150 اختبار ناجح) وجاهز تقنيًا.**
> **المانع الوحيد للتشغيل الحقيقي هو إجراءات منصّة Meta:** ملء الإعدادات الأساسية بالروابط القانونية،
> إعداد Webhooks على `bareeqplatform.site` والاشتراك بحقل `feed`، ثم **نشر التطبيق (Live)** —
> لأن Meta **لا ترسل أي أحداث حقيقية للتطبيقات غير المنشورة إطلاقًا**.
>
> بعد النشر، وبشرط أن يكون الطابور (`queue`) شغّالًا، سيعمل النظام كاملًا من التعليق حتى الرد.
